#ifndef __TETON_UTILS_UTILS_HPP__
#define __TETON_UTILS_UTILS_HPP__

#include <string>

namespace teton {
namespace utils {

bool getEnvVar(std::string env, std::string &result);

}  // namespace utils
}  // namespace teton

#endif
